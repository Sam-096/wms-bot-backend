spring:
  datasource:
    # ✅ Format: ${ENV_VAR:default_value_if_not_set}
    url: ${DB_URL:jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require}
    username: ${DB_USER:postgres.yhftnnhsptmuvpdxxrqc}
    password: ${DB_PASSWORD:fh*y$&Y#4_j4@QT}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      connection-timeout: 20000
      idle-timeout: 300000
      max-lifetime: 900000
      keepalive-time: 60000
      connection-test-query: SELECT 1
      initialization-fail-timeout: -1

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        boot:
          allow_jdbc_metadata_access: false # ← fixes the deprecation warning too
        jdbc:
          time_zone: UTC

  sql:
    init:
      mode: never

# ── Server ────────────────────────────────────────────────────────────────
server:
  port: ${PORT:8080}
  compression:
    enabled: true

# ── JWT ───────────────────────────────────────────────────────────────────
jwt:
  secret: ${JWT_SECRET:dGhpcyBpcyBhIHNlY3JldCBrZXkgZm9yIGRldmVsb3BtZW50IG9ubHk=}
  expiry-hours: ${JWT_EXPIRY_HOURS:24}
  refresh-expiry-days: ${JWT_REFRESH_EXPIRY_DAYS:7}

# ── Ollama ────────────────────────────────────────────────────────────────
ollama:
  base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
  model: llama3.2:3b
  temperature: 0.15
  top_p: 0.85

# ── Groq (Tier-2 cloud fallback) ──────────────────────────────────────────
groq:
  api-key: ${GROQ_API_KEY:}
  base-url: https://api.groq.com/openai/v1
  model: llama3-8b-8192
  timeout: 8000

# ── Sarvam AI ─────────────────────────────────────────────────────────────
sarvam:
  api:
    key: ${SARVAM_API_KEY:}
    base-url: "https://api.sarvam.ai" # FIXED: plain string, no markdown
  model: sarvam-m
  timeout: 5000

# ── CORS ──────────────────────────────────────────────────────────────────
cors:
  allowed-origins: "http://localhost:4200,${FRONTEND_URL:https://wmsai.netlify.app}"

# ── AI Provider ───────────────────────────────────────────────────────────
ai:
  provider: ${AI_PROVIDER:ollama}

# ── Intent Config ─────────────────────────────────────────────────────────
intent:
  quick-response:
    enabled: true

# ── Actuator ──────────────────────────────────────────────────────────────
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

# ── Logging ───────────────────────────────────────────────────────────────
logging:
  level:
    root: WARN
    com.wnsai: INFO
    org.springframework.web: WARN
    com.zaxxer.hikari: WARN
